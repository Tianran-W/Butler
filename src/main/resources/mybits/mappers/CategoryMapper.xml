<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.inventory.category.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="com.example.inventory.category.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="sortOrder" column="sort_order"/>
    </resultMap>

    <select id="findById" resultMap="CategoryResultMap">
        SELECT id, name, parent_id, sort_order FROM categories WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="CategoryResultMap">
        SELECT id, name, parent_id, sort_order FROM categories ORDER BY parent_id ASC NULLS FIRST, sort_order ASC
    </select>

    <select id="findByParentId" resultMap="CategoryResultMap">
        SELECT id, name, parent_id, sort_order 
        FROM categories 
        WHERE parent_id = #{parentId} 
        ORDER BY sort_order ASC
    </select>
    
    <select id="countChildrenByParentId" resultType="int">
        SELECT COUNT(*) FROM categories
        <where>
            <if test="parentId == null">
                parent_id IS NULL
            </if>
            <if test="parentId != null">
                parent_id = #{parentId}
            </if>
        </where>
    </select>

    <insert id="insertCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO categories (name, parent_id, sort_order)
        VALUES (#{name}, #{parentId}, #{sortOrder})
    </insert>

    <update id="updateParentAndSortOrder">
        UPDATE categories
        SET parent_id = #{parentId}, sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <update id="updateSortOrder">
        UPDATE categories
        SET sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <!-- 其他SQL语句 -->
</mapper>